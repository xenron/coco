# coco

# install
Node.js v4.4.5 x64
Anaconda2 x64

npm install -g bower
npm install -g mocha
npm install -g grunt-cli
npm install -g express-generator

pip install flask-script
pip install pandas

# run in /static/
bower install

